import 'package:flutter/material.dart';

class CallG extends StatelessWidget {
  const CallG({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
