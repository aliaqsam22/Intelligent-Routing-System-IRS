import 'package:flutter/material.dart';

class PhysioG extends StatelessWidget {
  const PhysioG({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.brown,
    );
  }
}