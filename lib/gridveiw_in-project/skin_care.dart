import 'package:flutter/material.dart';

class MySkin extends StatelessWidget {
  const MySkin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.deepOrange,
    );
  }
}
